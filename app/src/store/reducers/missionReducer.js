const initialState = {
    quotes: [],
    isLoading: false,
    error: ''
};

export const missionReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}